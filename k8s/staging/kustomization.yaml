apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ecommerce-staging

resources:
  # Base
  - namespace.yaml
  - configmap.yaml

  # Services
  - user-service.yaml
  - product-service.yaml
  - cart-service.yaml
  - order-service.yaml
  - payment-service.yaml
  - notification-service.yaml
  - health-service.yaml

  # Ingress
  - traefik-entrypoint.yaml
  - ingress.yaml
